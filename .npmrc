# NPM Configuration for project
# Avoid using sudo for installs
unsafe-perm=true

# Set registry
registry=https://registry.npmjs.org/

# Reduce noise
loglevel=warn

# Disable audit for faster installs
audit=false

# Use exact versions
save-exact=true